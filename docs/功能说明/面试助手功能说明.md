# 面试助手功能说明书

> **目标读者**: 产品经理、后端开发人员、运维人员
>
> **文档用途**: 详细说明面试助手的交互逻辑、技术架构、数据结构、隐私安全及配置部署
>
> **更新日期**: 2026-01-23
>
> **文档版本**: v1.1.0

------

## 📋 目录

- [项目简介](https://www.google.com/search?q=%23-项目简介)
- [使用场景与操作指南](https://www.google.com/search?q=%23-使用场景与操作指南)
- [核心架构](https://www.google.com/search?q=%23-核心架构)
- [模块详细说明](https://www.google.com/search?q=%23-模块详细说明)
- [数据结构与隐私](https://www.google.com/search?q=%23-数据结构与隐私)
- [配置与部署](https://www.google.com/search?q=%23-配置与部署)
- [常见问题 (FAQ)](https://www.google.com/search?q=%23-常见问题-faq)

------

## 📝 项目简介

**面试助手** 是一个基于企业微信智能机器人协议开发的后端服务。它连接了企业内部的面试数据库，允许 HR 或面试官通过自然语言指令，在企业微信聊天窗口中实时查询候选人信息。

### 核心能力

1. **消息回调处理**：验证企业微信签名并解密消息（基于 AES-256 算法）。
2. **指令识别**：支持 "今日面试" 和 "查看最近 N" 指令。
3. **隐私保护**：自动对输出的手机号和身份证进行脱敏处理。
4. **持久化存储**：使用 MySQL + MyBatis 管理面试数据。

------

## 📱 使用场景与操作指南

### 1. 适用场景

- **HR 早会**：快速拉取今日待面试人员名单，安排接待工作。
- **面试官查询**：在面试群中快速回顾最近几位候选人的简历摘要，无需登录 PC 后台。

### 2. 操作入口

- **入口**：企业微信客户端（PC端/移动端）。
- **位置**：添加了“面试助手”机器人的群聊，或直接与机器人单聊。

### 3. 指令交互演示

#### 场景 A：查询今日面试名单

**用户输入**：

> @面试助手 今日面试

**系统回复**：

Plaintext

```
今日已记录面试：2 人

1. 牧文｜南开大学 大四｜Frontend, React, Vue, TypeScript
2. 裴杰超｜武汉大学 大三｜Node.js, Express, MongoDB
```

#### 场景 B：查询最近录入记录

**用户输入**：

> @面试助手 查看最近 3

**系统回复**：

Plaintext

```
最近 3 条面试记录：

1.  姓名 牧文｜南开大学 大四
    技术栈 Frontend, React, Vue, TypeScript
    摘要 深入理解计算机系统，C++功底深厚...
    手机号 139xxxx1810
    身份证 110101xxxxxxxx6527
    面试时间 01-23 12:06
...
```

------

## 🏗️ 核心架构

### 1. 代码层级结构

本项目遵循标准的 Spring Boot 分层架构，严格遵守 `WxFbsir-business` 模块划分规范。

Plaintext

```
com.wx.fbsir.business/interviewbot
├── controller/      # 控制层：处理 HTTP 请求 (WechatBotController)
├── service/         # 业务层：指令解析与数据查询 (InterviewService)
├── mapper/          # 持久层：MyBatis 接口 (InterviewMapper)
├── domain/          # 实体层：数据对象与脱敏逻辑 (InterviewRecord)
├── utils/           # 工具层：加解密工具 (WXBizJsonMsgCrypt)
```

### 2. 数据流转图

Plaintext

```
用户发送消息 -> 企业微信服务器 -> /api/wechat (POST)
       -> WechatBotController (验证签名)
       -> WXBizJsonMsgCrypt (解密 JSON)
       -> 指令清洗 (去除 @机器人)
       -> InterviewService (业务查询)
       -> InterviewMapper (SQL 查询)
       -> InterviewRecord (数据脱敏)
       -> WXBizJsonMsgCrypt (加密回复)
       -> 返回企业微信 -> 用户收到回复
```

------

## 🧩 模块详细说明

### 1. 消息加解密 (Utils)

核心类：`com.wx.fbsir.business.interviewbot.utils.WXBizJsonMsgCrypt`

该类复刻自企业微信官方 Python Demo，负责处理 JSON 协议的加解密。

**关键特性**：

- 使用 Java 原生 `java.util.Base64` 替代 Apache Commons Codec，避免 Tomcat 依赖冲突。
- 实现了 `VerifyURL` (GET 验证) 和 `DecryptMsg`/`EncryptMsg` (POST 交互)。

**调用示例**：

Java

```
// 解密示例
WXBizJsonMsgCrypt wxcpt = new WXBizJsonMsgCrypt(sToken, sEncodingAESKey, sCorpID);
String decryptedMsg = wxcpt.DecryptMsg(msgSignature, timestamp, nonce, postData);
```

### 2. 控制器逻辑 (Controller)

核心类：`com.wx.fbsir.business.interviewbot.controller.WechatBotController`

处理 `/api/wechat` 的请求。包含指令清洗逻辑，支持容错匹配。

**指令正则逻辑**：

Java

```
// 匹配 "查看最近 N" (支持中间包含空格)
java.util.regex.Pattern pattern = java.util.regex.Pattern.compile("^查看最近\\s*(\\d+)$");
```

### 3. 业务与持久层 (Service & Mapper)

- **Service**: `InterviewService` 负责参数校验（如限制查询数量最大为 5）和结果格式化。
- **Mapper**: `InterviewMapper` 使用注解式 SQL，包含时间过滤逻辑。

**SQL 逻辑**：

Java

```
// SQL 示例：查询最近记录，排除未来时间，防止测试数据干扰
@Select("SELECT * FROM interview_record WHERE interview_time <= NOW() ORDER BY interview_time DESC LIMIT #{limit}")
List<InterviewRecord> selectRecentRecords(int limit);
```

------

## 🔒 数据结构与隐私

### 1. 数据库设计

表名：`interview_record`

| **字段名**       | **类型**     | **必填** | **说明**              |
| ---------------- | ------------ | -------- | --------------------- |
| `id`             | BIGINT       | 是       | 主键 ID               |
| `document_id`    | VARCHAR(64)  | 否       | 文档唯一标识          |
| `name`           | VARCHAR(32)  | 是       | 候选人姓名            |
| `school`         | VARCHAR(64)  | 否       | 学校                  |
| `grade`          | VARCHAR(32)  | 否       | 年级                  |
| `tech_stack`     | VARCHAR(255) | 否       | 技术栈                |
| `summary`        | TEXT         | 否       | 简历摘要              |
| `phone_number`   | VARCHAR(32)  | 否       | **敏感** 手机号       |
| `id_card`        | VARCHAR(32)  | 否       | **敏感** 身份证号     |
| `interview_time` | DATETIME     | 是       | 面试时间 (索引)       |
| `create_by`      | VARCHAR(64)  | 否       | 创建者 (默认: system) |
| `create_time`    | DATETIME     | 是       | 创建时间 (自动生成)   |
| `update_by`      | VARCHAR(64)  | 否       | 更新者 (默认: system) |
| `update_time`    | DATETIME     | 是       | 更新时间 (自动更新)   |
| `remark`         | VARCHAR(500) | 否       | 备注                  |

### 2. 隐私脱敏规范

在 `InterviewRecord` 实体类中实现了脱敏逻辑。为了避免 Markdown 渲染冲突（`*` 号会被解析为加粗），统一使用 `x` 作为掩码。

**手机号脱敏**：

Java

```
/**
 * 保留前3后4，中间4位掩盖
 * 例：13800138000 -> 138xxxx8000
 */
private String maskPhoneNumber(String phone) {
    if (phone == null || phone.length() < 11) return phone;
    return phone.replaceAll("(\\d{3})\\d{4}(\\d{4})", "$1xxxx$2");
}
```

**身份证脱敏**：

Java

```
/**
 * 保留前6后4，中间8位掩盖
 * 例：110101199001011234 -> 110101xxxxxxxx1234
 */
private String maskIdCard(String id) {
    if (id == null || id.length() < 15) return id;
    return id.replaceAll("(\\w{6})\\w+(\\w{4})", "$1xxxxxxxx$2");
}
```

------

## ⚙️ 配置与部署

### 1. 配置文件

在 `application.yml` 或启动参数中必须配置以下参数，否则无法解密消息或连接数据库：

YAML

```
wechat:
  token: "您的Token"            # 企业微信后台配置的 Token
  aes-key: "您的EncodingAESKey" # 企业微信后台配置的 EncodingAESKey
  corp-id: "您的企业ID"         # 企业 CorpID

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/interview_bot?useUnicode=true&characterEncoding=utf8
    username: root
    password: password
```

### 2. 日志规范

项目使用 SLF4J 记录日志，严格区分日志级别：

- `ERROR`: 异常堆栈（如解密失败、数据库连接错误）。
- `WARN`: 业务警告（如指令格式错误、不支持的消息类型）。
- `INFO`: 关键业务节点（如收到文本指令、指令处理成功）。
- `DEBUG`: 调试信息（如原始加密报文、清洗后的指令，生产环境建议关闭）。

------

## ❓ 常见问题 (FAQ)

**Q1: 为什么发送指令后机器人没反应？**

- **检查**：确认后端服务日志中是否有 `[微信回调] 收到原始消息`。
- **排查**：如果日志提示 `签名不匹配`，请检查 `token` 配置是否与企业微信后台一致。

**Q2: 为什么“查看最近 10”提示不支持？**

- **原因**：为了避免群聊消息过长刷屏，代码硬性限制了 `limit <= 5`。

**Q3: 收到 504 Gateway Timeout 怎么办？**

- **原因**：如果处理时间超过微信限制（5秒），微信会重试。本项目采用了同步回复，通常处理很快。如果出现超时，请检查数据库连接是否缓慢。

------

**最后更新**: 2026-01-23

**文档版本**: v1.1.0