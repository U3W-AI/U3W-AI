<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wx.fbsir.business.airobotmessage.mapper.MessageMapper">
    <resultMap id="BaseResultMap" type="com.wx.fbsir.business.airobotmessage.domain.WecomWebhook">
        <id column="id" property="id"/>
        <result column="enterprise_id" property="enterpriseId"/>
        <result column="name" property="name"/>
        <result column="webhook_url" property="webhookUrl"/>
        <result column="webhook_secret_ref" property="webhookSecretRef"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="version" property="version"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="columns">
        id, enterprise_id, name, webhook_url, webhook_secret_ref, description,
        status, version, create_time, update_time
    </sql>

    <select id="selectWecomWebhookList" resultMap="BaseResultMap">
        SELECT <include refid="columns"/>
        FROM wc_webhook_url
        WHERE enterprise_id = #{enterpriseId}
        ORDER BY id
    </select>

    <select id="selectWecomWebhookById" resultMap="BaseResultMap">
        SELECT <include refid="columns"/>
        FROM wc_webhook_url
        WHERE id = #{id} AND enterprise_id = #{enterpriseId}
        LIMIT 1
    </select>

    <insert id="insertWecomWebhook" parameterType="com.wx.fbsir.business.airobotmessage.domain.WecomWebhook"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wc_webhook_url
          (enterprise_id, name, webhook_url, webhook_secret_ref, description, status, version)
        VALUES
          (#{enterpriseId}, #{name}, #{webhookUrl}, #{webhookSecretRef}, #{description}, #{status}, 1)
    </insert>

    <update id="updateWecomWebhook" parameterType="com.wx.fbsir.business.airobotmessage.domain.WecomWebhook">
        UPDATE wc_webhook_url
        <set>
            name = #{name},
            <if test="webhookSecretRef != null">
                webhook_url = #{webhookUrl}, webhook_secret_ref = #{webhookSecretRef},
            </if>
            description = #{description}, status = #{status},
            version = version + 1, update_time = NOW()
        </set>
        WHERE id = #{id} AND enterprise_id = #{enterpriseId} AND version = #{version}
    </update>

    <delete id="deleteWecomWebhookById">
        DELETE FROM wc_webhook_url
        WHERE id = #{id} AND enterprise_id = #{enterpriseId} AND version = #{version}
    </delete>
</mapper>
