<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wx.fbsir.business.websocket.mapper.WsIpBlacklistMapper">

    <resultMap type="com.wx.fbsir.business.websocket.domain.WsIpBlacklist" id="WsIpBlacklistResult">
        <result property="id" column="id"/>
        <result property="ipAddress" column="ip_address"/>
        <result property="blockReason" column="block_reason"/>
        <result property="blockType" column="block_type"/>
        <result property="expireTime" column="expire_time"/>
        <result property="hitCount" column="hit_count"/>
        <result property="lastHitTime" column="last_hit_time"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectBlacklistVo">
        SELECT id, ip_address, block_reason, block_type, expire_time, hit_count, last_hit_time,
               status, remark, del_flag, create_by, create_time, update_by, update_time
        FROM ws_ip_blacklist
    </sql>

    <select id="selectById" parameterType="Long" resultMap="WsIpBlacklistResult">
        <include refid="selectBlacklistVo"/>
        WHERE id = #{id}
    </select>

    <select id="selectByIpAddress" parameterType="String" resultMap="WsIpBlacklistResult">
        <include refid="selectBlacklistVo"/>
        WHERE ip_address = #{ipAddress} AND del_flag = 0
    </select>

    <select id="selectActiveByIpAddress" parameterType="String" resultMap="WsIpBlacklistResult">
        <include refid="selectBlacklistVo"/>
        WHERE ip_address = #{ipAddress} 
          AND status = 1 
          AND del_flag = 0
          AND (expire_time IS NULL OR expire_time > NOW())
    </select>

    <select id="selectList" parameterType="com.wx.fbsir.business.websocket.domain.WsIpBlacklist" resultMap="WsIpBlacklistResult">
        <include refid="selectBlacklistVo"/>
        <where>
            del_flag = 0
            <if test="ipAddress != null and ipAddress != ''">
                AND ip_address LIKE CONCAT('%', #{ipAddress}, '%')
            </if>
            <if test="blockType != null">
                AND block_type = #{blockType}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.wx.fbsir.business.websocket.domain.WsIpBlacklist" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ws_ip_blacklist (
            ip_address, block_reason, block_type, expire_time, hit_count, 
            status, remark, del_flag, create_by, create_time
        ) VALUES (
            #{ipAddress}, #{blockReason}, #{blockType}, #{expireTime}, 0,
            #{status}, #{remark}, 0, #{createBy}, NOW()
        )
    </insert>

    <update id="update" parameterType="com.wx.fbsir.business.websocket.domain.WsIpBlacklist">
        UPDATE ws_ip_blacklist
        <set>
            <if test="blockReason != null">block_reason = #{blockReason},</if>
            <if test="blockType != null">block_type = #{blockType},</if>
            <if test="expireTime != null">expire_time = #{expireTime},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <update id="deleteById" parameterType="Long">
        UPDATE ws_ip_blacklist SET del_flag = 1, update_time = NOW() WHERE id = #{id}
    </update>

    <update id="updateHitCount" parameterType="Long">
        UPDATE ws_ip_blacklist 
        SET hit_count = hit_count + 1, last_hit_time = NOW(), update_time = NOW() 
        WHERE id = #{id}
    </update>

    <update id="unblockByIpAddress" parameterType="String">
        UPDATE ws_ip_blacklist 
        SET status = 0, update_time = NOW() 
        WHERE ip_address = #{ipAddress} AND del_flag = 0
    </update>

</mapper>
