# 福帮手完整部署文档（Windows PowerShell）

> 文档版本：2.0.0
> 更新日期：2026-07-11

本文给出从 GitHub 源码到本地可登录系统、Admin 与 Engine 联通的唯一公开部署路径。命令以 Windows PowerShell 为准；源码、构建文件和 `scripts/init-database.ps1` 是部署事实来源。历史文档如与本文冲突，以当前源码和本文为准。

## 1. 部署边界

本地基础闭环包括 MySQL、Redis、Admin 和 UI。Engine 是浏览器自动化与受控执行节点，需要验证编排能力时再启动。

以下均属于外部第三方边界，不是基础本地启动的前提：Gitee OAuth、企业微信智能机器人与 Webhook、腾讯元器、OpenClaw、第三方 AI 服务、内网穿透。首次拉取源码、Maven/npm 依赖及 Playwright 浏览器组件仍需网络。

## 2. 环境准备

建议环境：

- Windows 10/11
- Git
- JDK 17
- Maven 3.8 或更高版本
- MySQL 8.0
- Redis 6.0 或更高版本
- Node.js 18 或更高版本，npm 9 或更高版本

验证安装：

```powershell
git --version
java -version
mvn -version
mysql --version
redis-cli --version
redis-server --version
node -v
npm -v
```

检查默认端口占用：

```powershell
Get-NetTCPConnection -LocalPort 80,8080,8081 -ErrorAction SilentlyContinue
```

## 3. 获取源码

```powershell
git clone --branch fbsir --single-branch https://github.com/U3W-AI/U3W-AI.git
Set-Location .\U3W-AI
```

也可以在 GitHub 的 `fbsir` 分支页面选择 **Code → Download ZIP**，解压后在该目录打开 PowerShell。后续命令均从仓库根目录执行。

## 4. 初始化数据库

数据库技术名称固定为 `wxfbsir`，这是兼容既有系统的内部标识，不随品牌名变化。

### 4.1 保存 MySQL 本机登录信息

初始化脚本不接收明文密码。使用 MySQL 自带的登录路径保存本机凭据：

```powershell
mysql_config_editor set --login-path=fbsir-local --host=127.0.0.1 --port=3306 --user=root --password
```

命令会交互式询问密码，不会把密码写进本文、命令历史或仓库。

### 4.2 核对并执行固定清单

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\init-database.ps1 -LoginPath fbsir-local -DryRun
powershell -ExecutionPolicy Bypass -File .\scripts\init-database.ps1 -LoginPath fbsir-local
```

脚本按固定顺序执行 26 个步骤：基础库、`quartz.sql`、`Resume.sql` 和 23 个增量脚本。它会安全解析包含中文的 SQL 文件名，任一步失败立即停止，并在 `u3w_schema_migration` 中记录 `RUNNING`、`APPLIED` 或 `FAILED` 状态。

当前公开清单顺序如下，不能按文件名字典序执行：

1. `wxfbsir.sql`
2. `quartz.sql`
3. `Resume.sql`
4. `update_20260129_插入giteeAI助手.sql`
5. `update_20260303_新建系统提示词表和Webhook地址表.sql`
6. `update_20260316_为白名单表添加在线状态，主机类型，健康检查字段.sql`
7. `update_20260321_输出物权限.sql`
8. `update_20260408_权益基础建表.sql`
9. `update_20260409_企业场景包建表.sql`
10. `update_20260409_平台场景包运营菜单.sql`
11. `update_20260410_用户自助服务_审计字段.sql`
12. `update_20260410_企业场景包运营菜单.sql`
13. `update_20260411_技能API网关建表.sql`
14. `update_20260411_用户自助服务菜单.sql`
15. `update_20260413_企微CLI最小可行产品建表.sql`
16. `update_20260416_积分模型适配_添加event_id.sql`
17. `update_20260417_前端APIKey管理_添加字段.sql`
18. `update_20260417_用户APIKey菜单.sql`
19. `update_20260420_SkillAPI安全_字段扩容.sql`
20. `update_20260710_修复FBS运营菜单父节点.sql`
21. `update_20260711_FBSir定时任务品牌兼容迁移.sql`
22. `update_20260711_企微智能机器人编排控制面建表.sql`
23. `update_20260711_企微智能机器人编排加密内容工件.sql`
24. `update_20260711_企微智能机器人编排Outbox租约增强.sql`
25. `update_20260711_Webhook枢纽安全与投递台账.sql`
26. `update_20260711_企微智能机器人调度任务.sql`

特别是第 24 步依赖第 22 步建立控制面表，按名称排序会提前执行并失败。不要在 Windows MySQL 客户端中手工 `SOURCE` 这些中文文件名；统一使用初始化脚本按原始字节传入 SQL。

重要规则：

- 已成功的步骤可安全跳过；失败或中断的 DDL 不会被盲目重放。
- 如果现有 `wxfbsir` 库非空但没有初始化回执，脚本会拒绝执行；请使用新库或经审核的升级流程。
- 仓库没有 `gitee.sql`，不要执行旧文档中的该命令。
- `quartz.sql` 必须紧跟基础库执行，用于创建 Quartz 调度器表；后续品牌兼容和调度任务迁移依赖这组基础表。

验证结果：

```powershell
mysql --login-path=fbsir-local --database=wxfbsir --execute="SELECT version, description FROM u3w_schema_migration ORDER BY version;"
mysql --login-path=fbsir-local --database=wxfbsir --execute="SELECT COUNT(*) AS table_count FROM information_schema.tables WHERE table_schema='wxfbsir';"
```

第一条查询应显示 `public_init_001` 至 `public_init_026`，且全部为 `APPLIED:` 状态。

## 5. 配置 Admin

Redis 是登录、验证码和令牌流程的必需依赖，不是可选缓存。先启动 Redis，再验证：

```powershell
redis-cli ping
```

应返回 `PONG`。Windows 可使用 WSL、Docker 或可信的兼容 Redis 服务；具体发行方式属于外部运行环境选择。

在即将启动 Admin 的同一个 PowerShell 窗口执行以下配置。示例只包含占位符和本机生成的随机密钥：

```powershell
function New-FBSirSecret {
    param([int]$Bytes = 32)
    $buffer = New-Object byte[] $Bytes
    $rng = [Security.Cryptography.RandomNumberGenerator]::Create()
    try {
        $rng.GetBytes($buffer)
        [Convert]::ToBase64String($buffer)
    }
    finally {
        $rng.Dispose()
    }
}

$env:FBSIR_MYSQL_URL = 'jdbc:mysql://127.0.0.1:3306/wxfbsir?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8&allowMultiQueries=true'
$env:FBSIR_MYSQL_USERNAME = '<YOUR_DB_USER>'
$env:FBSIR_MYSQL_PASSWORD = '<YOUR_DB_PASSWORD>'
$env:FBSIR_REDIS_HOST = '127.0.0.1'
$env:FBSIR_REDIS_PORT = '6379'
$env:FBSIR_REDIS_DATABASE = '0'
$env:FBSIR_REDIS_PASSWORD = ''
$env:FBSIR_AES_SECRET_KEY = New-FBSirSecret
$env:FBSIR_TOKEN_SECRET = New-FBSirSecret
$env:FBSIR_ENGINE_TOKEN = New-FBSirSecret
$env:FBSIR_DOMAIN = 'http://localhost:8080'
$env:FBSIR_FILE_PATH = 'D:/FBSir/uploadPath'
$env:FBSIR_WEBHOOK_KEY_VERSION = 'v1'
$env:FBSIR_WEBHOOK_SECRET_KEY_V1 = New-FBSirSecret
$env:FBSIR_DRUID_STAT_ENABLED = 'false'

New-Item -ItemType Directory -Force -Path $env:FBSIR_FILE_PATH | Out-Null
```

将两个数据库占位符替换为本机真实值。若 Redis 配置了密码，再设置 `FBSIR_REDIS_PASSWORD`。

Engine 与 Admin 必须共享同一令牌。在终端 A 中把本次生成的值临时复制到剪贴板，粘贴到第 7 节的本地 Engine 配置后立即清空剪贴板：

```powershell
$env:FBSIR_ENGINE_TOKEN | Set-Clipboard
# 完成粘贴后执行
Set-Clipboard -Value ''
```

配置约束：

- 一旦设置任意非空 `FBSIR_*` 变量，Admin 就要求同一进程中完整提供 `MYSQL_URL`、`MYSQL_USERNAME`、`MYSQL_PASSWORD`、`REDIS_HOST`、`REDIS_PORT`、`REDIS_DATABASE`、`AES_SECRET_KEY`、`TOKEN_SECRET`、`ENGINE_TOKEN`、`DOMAIN` 和 `FILE_PATH`。
- `FBSIR_WEBHOOK_KEY_VERSION` 与对应的 `FBSIR_WEBHOOK_SECRET_KEY_V1` 是 Webhook 加密功能的版本化配置。
- 本地 Vite 开发代理会把 WebSocket 握手 Origin 安全地改写为 Admin 目标地址，以配合后端默认同源策略。若部署时 UI 与 Admin 确实分属不同 Origin 且不经过同源网关，应额外设置 `FBSIR_WEBSOCKET_ALLOWED_ORIGINS` 为逗号分隔的精确 Origin 列表，禁止使用 `*`。
- 历史 `WXFBSIR_*` 仅用于完整的旧部署升级，不能与新变量族拼接或混用。
- 这些变量只在当前 PowerShell 进程有效；关闭窗口后需重新配置或由安全的部署系统注入。
- 不要把密钥、数据库密码或第三方凭据提交到 Git。

可选的企微智能机器人加密配置需由外部密钥系统生成并按功能文档注入：`SMARTBOT_IDENTITY_HMAC_KEY_BASE64` 要求 Base64 解码后至少 32 字节，`SMARTBOT_INPUT_ENCRYPTION_KEY_V1` 要求 Base64 解码后正好 32 字节。基础本地启动不需要它们。

## 6. 构建项目

构建后端 Reactor：

```powershell
mvn clean install
```

只构建 Admin 及其依赖也可以使用：

```powershell
mvn -pl FBSir-admin -am clean package
```

Engine 不在根 Maven Reactor 中，需要单独构建：

```powershell
mvn -f .\FBSir-engine\pom.xml clean package
```

## 7. 三个终端启动

### 终端 A：Admin（8080）

必须在已经设置完整 `FBSIR_*` 变量的同一窗口中启动：

```powershell
mvn -f .\FBSir-admin\pom.xml spring-boot:run
```

### 终端 B：Engine（8081）

Engine 明确屏蔽系统环境变量、JVM 系统属性和命令行属性覆盖。运行目录中的外部 `application.yml` 是安全配置入口；不要把真实令牌写回源码。

创建仓库已忽略的本地运行目录：

```powershell
New-Item -ItemType Directory -Force -Path .\fbsir\engine | Out-Null
Copy-Item .\FBSir-engine\target\classes\application.yml .\fbsir\engine\application.yml -Force
notepad .\fbsir\engine\application.yml
```

在外部文件中确认以下配置；`engine-token` 必须与终端 A 的 `FBSIR_ENGINE_TOKEN` 完全一致且至少 32 个字符：

```yaml
fbsir:
  engine:
    ws-url: ws://localhost:8080/ws/engine
    host-id: engine-001
    engine-token: "<SAME_VALUE_AS_ADMIN_FBSIR_ENGINE_TOKEN>"
    playwright:
      enabled: true
      data-dir: ./data/playwright
      headless: false
```

完整 26 步数据库初始化会准备 `engine-001` 及其 Engine 类型字段。随后从该运行目录启动精确产物：

```powershell
Push-Location .\fbsir\engine
java -jar ..\..\FBSir-engine\target\FBSir-engine-1.3.1.jar
Pop-Location
```

首次实际启动 Playwright 浏览器时可能下载浏览器组件并建立本机缓存，这属于外部网络与磁盘边界。配置兼容逻辑可先独立验证：

```powershell
mvn -f .\FBSir-engine\pom.xml -Dtest=FBSirConfigurationAliasEnvironmentPostProcessorTest test
```

### 终端 C：UI（默认 80）

开发模式必须保留 `/dev-api`，并让 Vite 代理指向 Admin：

```dotenv
VITE_APP_BASE_API = '/dev-api'
VITE_APP_PROXY_TARGET = 'http://localhost:8080'
```

启动：

```powershell
Set-Location .\FBSir-ui
npm ci
npm run dev
```

`vite.config.js` 中的 Origin 改写只服务于本地开发代理，不应把 Vite 开发服务器暴露为公网网关。生产部署应让 UI 与 `/ws/client` 经过同一公开 Origin，或按上一节显式配置允许来源。

默认访问 `http://localhost:80`。若 80 端口被占用：

```powershell
npm run dev -- --port 8082
```

此时访问 `http://localhost:8082`，API 仍由代理转发到 8080。

## 8. 验证闭环

在第四个 PowerShell 窗口执行：

```powershell
Invoke-WebRequest http://localhost:8080 -UseBasicParsing
Invoke-RestMethod http://localhost:8081/actuator/health
Invoke-WebRequest http://localhost:80 -UseBasicParsing
```

若 UI 使用 8082，则把最后一条改为该端口。使用数据库初始化数据中的管理账号登录，并在首次登录后立即修改密码；本文不公开或固化默认密码。

登录后进入“主机管理”，确认 `engine-001` 在线，再执行一次受控的 Engine 健康检查。至此形成 UI → Admin → MySQL/Redis → WebSocket → Engine 的本地闭环。

## 9. 常见问题

### Admin 启动即报告变量族不完整

当前窗口存在部分 `FBSIR_*` 变量。补齐第 5 节的全部必需项，或清除整套变量并改用经过审核的外部配置文件；不要混搭旧变量。

### 登录页提示后端连接异常

确认 Admin 与 Redis 都已启动，并检查端口及日志：

```powershell
Get-NetTCPConnection -LocalPort 6379,8080 -ErrorAction SilentlyContinue
Get-Process | Where-Object { $_.ProcessName -match 'java|redis' }
Get-Content .\FBSir-admin\logs\sys-info.log -Tail 100 -ErrorAction SilentlyContinue
```

### Engine 无法连接 Admin

检查 Admin 是否监听 8080、`ws-url` 是否为 `/ws/engine`、`engine-token` 是否完全一致，以及 `engine-001` 是否在白名单中：

```powershell
Get-NetTCPConnection -LocalPort 8080,8081 -ErrorAction SilentlyContinue
Get-Process | Where-Object { $_.ProcessName -eq 'java' }
```

### 需要停止某个本地进程

先查明进程，再按实际 PID 停止：

```powershell
Get-NetTCPConnection -LocalPort 8080 -ErrorAction SilentlyContinue | Select-Object LocalPort,OwningProcess
Stop-Process -Id <PID>
```

## 10. 后续文档

- [Engine 专项说明](FBSir-engine/README.md)
- [文档中心](docs/README.md)
- [Playwright 框架完整指南](docs/功能说明/engine/Playwright框架完整指南.md)
- [WebSocket 通信完整指南](docs/功能说明/engine/WebSocket通信完整指南.md)
- [企微智能机器人消息功能说明](docs/功能说明/business功能/企业微信机器人消息功能说明.md)

第三方平台配置应在对应功能启用时单独完成，不应复制到基础部署流程中。
